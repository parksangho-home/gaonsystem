# 가온시스템 리뉴얼 기술 스택 결정 초안

> 상태: 검토 요청 전 초안  
> 목적: 가온시스템의 계측기 판매·대여 쇼핑몰을 위한 기술 조합을 결정한다. 본 문서는 구현 계획이 아니라, 착수 전 아키텍처 의사결정 기록(ADR)이다.

## 1. 전제와 범위

- 기존 가온시스템 홈페이지를 제품 탐색, 판매, 대여 및 기술 상담이 가능한 쇼핑몰로 리뉴얼한다.
- 일반 고객, 준회원, 정회원을 지원한다. 회원 할인율·승급 기준은 운영 화면에서 추후 설정한다.
- 초기 결제수단은 실시간 계좌이체와 현금영수증이다.
- 대여는 장비별 가용 재고, 기간 중복 방지, 출고·반납 점검, 보증금 및 파손 정산을 지원해야 한다.
- 관리자 기능은 상품, 판매 재고, 대여 재고, 주문, 대여, 회원 등급, 상담을 관리해야 한다.

## 2. 결정 기준

| 기준 | 우선순위 | 이유 |
| --- | --- | --- |
| 대여 재고 정합성 | 매우 높음 | 같은 장비가 겹치는 기간에 중복 대여되면 운영 손실이 발생한다. |
| 한국 결제 연동 | 매우 높음 | 계좌이체 완료 확인, 현금영수증, 취소·환불 처리가 필요하다. |
| 운영자 변경 편의성 | 높음 | 제품·사양·배너·등급 정책을 개발자 없이 자주 바꿔야 한다. |
| SEO·제품 탐색 성능 | 높음 | 계측기명·사양 기반 검색 유입과 빠른 제품 페이지가 중요하다. |
| 확장성 | 높음 | 향후 견적서, 사업자 회원, 검교정 이력, 세금계산서로 확장 가능해야 한다. |
| 초기 구축 속도 | 중간 | 빠른 출시가 필요하지만 대여 핵심 로직을 SaaS 기능에 억지로 맞추지 않는다. |

## 3. 후보 조합 비교

| 후보 | 장점 | 한계 | 판단 |
| --- | --- | --- | --- |
| 기존 아임웹 고도화 | 가장 빠르게 현재 사이트를 개선할 수 있다. | 대여 기간별 재고 잠금, 보증금·파손 정산, 맞춤 회원 정책을 구현할 때 제약이 커질 수 있다. | 랜딩·회사소개만이라면 가능하나, 본 쇼핑몰의 핵심에는 비추천 |
| Next.js + Supabase + PortOne | 판매·대여 도메인을 맞춤 설계하면서 인증·DB·파일 저장소를 빠르게 확보한다. | 초기 데이터 모델·권한·결제 검증을 개발해야 한다. | **권장** |
| Next.js + 독립 API 서버 + 관리형 PostgreSQL | 가장 큰 유연성과 확장성을 얻는다. | 인증·파일 저장·운영 도구까지 직접 구성해 초기 비용과 시간이 증가한다. | 대여량·조직·외부 연동이 크게 늘어날 때 2단계 후보 |

## 4. 권장 스택

| 계층 | 선택 | 역할 | 결정 이유 |
| --- | --- | --- | --- |
| 웹 프런트·BFF | Next.js (App Router) + TypeScript | 쇼핑몰 화면, SEO, 서버 렌더링, API 엔드포인트 | App Router는 파일 기반 라우팅과 서버 기능을 제공하며, Route Handler로 결제 웹훅·관리 API를 동일 프로젝트에서 처리할 수 있다. |
| UI | Tailwind CSS + shadcn/ui | 반응형 디자인 시스템·관리자 화면 | 컴포넌트 소유권을 프로젝트에 두고, 가온시스템의 산업기술 브랜드에 맞게 세밀히 수정한다. |
| 데이터베이스 | Supabase PostgreSQL | 상품, 재고, 주문, 대여, 등급, 상담 데이터 | 관계·제약·트랜잭션이 대여 재고와 주문 정합성에 적합하다. |
| 인증·권한 | Supabase Auth + PostgreSQL RLS | 회원, 관리자, 본인 주문·대여 접근 제어 | Auth와 RLS를 결합해 브라우저 접근에도 행 단위 권한을 적용한다. 관리자 권한은 서버에서 별도 확인한다. |
| 파일 | Supabase Storage | 제품 사진, 카탈로그, 교정 성적서(권한 필요) | DB·권한 모델과 함께 관리한다. 성적서 등 민감 문서는 공개 버킷에 두지 않는다. |
| 결제 | PortOne V2 + 계약 PG | 실시간 계좌이체, 현금영수증, 결제 검증·취소·웹훅 | 결제수단 및 PG를 교체·추가할 여지를 유지한다. |
| 배포 | Vercel | Next.js 배포, 프리뷰, 서버리스 실행 | 프런트와 BFF를 한 배포 흐름으로 운영한다. |
| 예약 작업 | Vercel Cron + 서버 작업 | 입금 미확인 만료, 대여 시작·반납 알림, 연체 상태 점검 | 시간 기반 운영 작업을 별도 서버 없이 시작한다. |
| 모니터링 | Sentry | 오류 추적, 결제 웹훅 실패 알림 | 결제·재고 오류를 빠르게 발견한다. |

## 5. 핵심 설계 원칙

### 5.1 판매와 대여를 분리해 모델링한다

- `products`: 공통 제품 정보, 사양, 브랜드, 공개 상태
- `sale_skus`: 판매가, 판매 재고, 납기, 옵션
- `rental_assets`: 실제 대여 장비 단위(시리얼 번호, 상태, 교정 만료일, 대체 조달가)
- `rental_reservations`: 대여 시작·종료일, 상태, 보증금, 반납·검수 결과
- `orders`, `order_items`, `payments`: 판매 주문·결제의 기준 데이터

대여 가능 여부는 단순 수량 차감이 아니라 `rental_assets`별 기간 겹침을 트랜잭션에서 검사해 확정한다. 예약 확정, 취소, 반납, 연장은 모두 서버 전용 함수 또는 API에서 수행한다. 브라우저가 재고·보증금·주문 상태를 직접 변경하면 안 된다.

### 5.2 권한은 서버와 DB에서 이중 확인한다

- 공개 상품·카테고리만 비회원 읽기를 허용한다.
- 고객은 본인의 주문, 대여, 상담만 조회한다.
- 준회원·정회원 여부와 할인율은 고객이 수정할 수 없는 `membership_tier` 데이터로 관리한다.
- 관리자·운영자 권한은 별도의 역할 테이블과 서버 측 권한 검사로 관리한다.
- 결제 비밀키, PortOne API Secret, Supabase service role 키는 서버 환경변수에만 둔다.

### 5.3 결제 완료는 브라우저 결과만 믿지 않는다

1. 서버가 주문 금액과 재고·대여 가능 여부를 확정한 뒤 결제 시도를 생성한다.
2. 브라우저는 PortOne 결제창을 호출한다.
3. 서버가 PortOne API로 결제 금액·주문번호·상태를 재검증한다.
4. 웹훅도 서명 검증 후 같은 상태 전이를 처리한다.
5. 검증이 모두 끝난 뒤에만 주문을 `PAID` 또는 대여를 `CONFIRMED` 상태로 바꾼다.

## 6. 초기 서비스 구조

```text
고객 브라우저
  └─ Next.js 웹 (상품, 장바구니, 대여 신청, 마이페이지, 관리자)
       ├─ Next.js Route Handlers
       │    ├─ PortOne 결제 검증 / 웹훅
       │    ├─ 대여 예약·연장·반납 상태 전이
       │    └─ 관리자 전용 변경 API
       ├─ Supabase Auth / PostgreSQL / Storage
       └─ PortOne V2 → 계약 PG → 실시간 계좌이체·현금영수증

Vercel Cron
  └─ 입금 미확인 만료 / 반납 알림 / 연체 점검
```

## 7. 이번 단계에서 의도적으로 제외하는 것

- 별도 마이크로서비스, Kubernetes, 메시지 큐
- Elasticsearch·Algolia: 초기에는 PostgreSQL 검색으로 시작하고 제품 수·검색 품질 요구가 커지면 도입한다.
- 네이티브 모바일 앱
- 복수 판매자 정산 기능
- 카드·간편결제: 이후 PortOne 결제수단 추가로 확장한다.
- 자동 세금계산서 발행: B2B 요구가 확정된 뒤 별도 검토한다.

## 8. 구현 착수 전 확인할 위험과 대응

| 위험 | 대응 |
| --- | --- |
| 대여 중복 예약 | DB 트랜잭션·기간 겹침 검사·장비 단위 자산 관리·관리자 수동 오버라이드 이력 |
| 결제 위변조·중복 처리 | 서버 금액 검증, 주문번호 유일성, PortOne 웹훅 검증, 멱등 처리 |
| 대여 장비 파손 분쟁 | 출고·반납 사진, 시리얼·구성품 점검표, 약관 동의, 검수 이력 보관 |
| 개인정보·성적서 노출 | RLS, 비공개 스토리지, 짧은 만료의 서명 URL, 최소 권한 |
| 외부 서비스 비용·종속 | 데이터는 PostgreSQL 표준으로 유지하고, 결제는 PortOne 추상화 계층으로 감싼다. |

## 9. 공식 문서 확인 근거

- [Next.js App Router](https://nextjs.org/docs/app): 파일 기반 라우팅과 서버 기능을 제공한다.
- [Next.js Route Handlers](https://nextjs.org/docs/app/getting-started/route-handlers): `app` 디렉터리에서 결제 웹훅과 백엔드 API를 구성할 수 있다.
- [Supabase Auth](https://supabase.com/docs/guides/auth): JWT 기반 인증과 RLS 연계를 제공한다.
- [Supabase RLS](https://supabase.com/docs/guides/database/postgres/row-level-security): 데이터베이스 행 단위 접근 제어와 서비스 역할 키의 서버 전용 사용 원칙을 안내한다.
- [PortOne V2 API](https://developers.portone.io/api/rest-v2): 결제·현금영수증 관련 API 카테고리를 제공한다.
- [PortOne 실시간 계좌이체](https://developers.portone.io/opi/ko/console/guide/payments?v=v2): 실시간 계좌이체 결제수단을 지원한다.
- [PortOne 결제 검증·웹훅 가이드](https://developers.portone.io/opi/ko/quick-guide/payment?v=v2): 서버 검증과 웹훅 검증을 권장한다.
- [Vercel Cron Jobs](https://vercel.com/docs/cron-jobs): Vercel Functions 기반 시간 예약 작업을 지원한다.

## 10. 검토 요청 사항

아래 세 가지 승인 여부가 확정되면 이 조합을 기술 스택 기준선으로 채택한다.

1. **권장안 채택:** Next.js + Supabase + PortOne + Vercel을 1차 구축 조합으로 승인할까요?
2. **운영 방식:** 운영자가 상품·사양·배너를 직접 수정할 수 있는 관리자 화면을 1차 범위에 포함할까요? (권장: 포함)
3. **개인정보·문서 보관:** 제품 카탈로그는 공개, 교정 성적서·출고 점검표는 고객·관리자 전용으로 분리하는 정책을 승인할까요? (권장: 승인)
